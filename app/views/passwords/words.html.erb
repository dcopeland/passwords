<turbo-frame id="words">
    <p onClick="reloadFrame(this.parentNode); return false">words</p>
    <ul>
        <% @passphrases.each do |passphrase| %>
            <li><%= passphrase %></li>
        <% end %>
    </ul>
</turbo-frame>