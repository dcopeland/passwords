<turbo-frame id="chars">
    <p onClick="reloadFrame(this.parentNode); return false">chars / digits</p>
    <ul>
        <% @passwords.each do |password| %>
            <li><%= password %></li>
        <% end %>
    </ul>
</turbo-frame>